{"decks":[{"id":"sql","label":"SQL & PL/SQL"},{"id":"heap","label":"Heap / Priority Queue"},{"id":"stack","label":"Stack"}],"cards":[{"id":"cs-53e9df0f","d":"sql","t":"Foundations","f":"Name the five SQL command families and one example of each.","b":"**DDL** (CREATE, ALTER, DROP, TRUNCATE) · **DML** (INSERT, UPDATE, DELETE) · **DQL** (SELECT) · **DCL** (GRANT, REVOKE) · **TCL** (COMMIT, ROLLBACK, SAVEPOINT)."},{"id":"cs-31b94153","d":"sql","t":"Foundations","f":"In Oracle, what is `''` (an empty string)?","b":"**NULL.** Oracle treats the empty string as NULL. In MySQL/PostgreSQL/SQL Server `''` is a real (empty) value."},{"id":"cs-1e596887","d":"sql","t":"Foundations","f":"What does `NULL = NULL` evaluate to, and how do you test for NULL?","b":"**UNKNOWN** (not TRUE). Use `IS NULL` / `IS NOT NULL`. A WHERE clause keeps only rows where the condition is TRUE."},{"id":"cs-9645634c","d":"sql","t":"Foundations","f":"What is three-valued logic? Give `NULL AND FALSE` and `NULL OR TRUE`.","b":"Conditions are TRUE, FALSE or UNKNOWN. `NULL AND FALSE` = FALSE; `NULL OR TRUE` = TRUE; `NULL AND TRUE` = UNKNOWN."},{"id":"cs-cebbf24b","d":"sql","t":"Foundations","f":"Oracle's main text and number types (not `VARCHAR`/`INT`)?","b":"`VARCHAR2(n)` (max 4000 bytes) for text, `NUMBER(p,s)` for numbers, `DATE` (includes a time part!), `CLOB`/`BLOB` for large objects."},{"id":"cs-0780267f","d":"sql","t":"Foundations","f":"Explain `NUMBER(7,2)`.","b":"Precision 7, scale 2: **7 digits in total, 2 after the decimal point** (so 5 before it). Max value 99999.99."},{"id":"cs-bdb025a7","d":"sql","t":"Foundations","f":"What is `DUAL` in Oracle?","b":"A one-row, one-column table used when you need a `SELECT` without a real table, e.g. `SELECT SYSDATE FROM dual;`."},{"id":"cs-8bfbc54c","d":"sql","t":"Foundations","f":"Operator precedence of `NOT`, `AND`, `OR`?","b":"`NOT` binds tightest, then `AND`, then `OR`. Use parentheses: `a OR b AND c` means `a OR (b AND c)`."},{"id":"cs-8e0f6d42","d":"sql","t":"Foundations","f":"How does `||` treat NULL in Oracle? What about `+` in SQL Server?","b":"Oracle `'abc' || NULL` = `'abc'` (NULL ≈ empty). SQL Server `'abc' + NULL` = **NULL**."},{"id":"cs-b22af2ac","d":"sql","t":"DDL and tables","f":"`DELETE` vs `TRUNCATE` vs `DROP` — differences?","b":"**DELETE**: DML, removes chosen rows (WHERE), can roll back, fires triggers. **TRUNCATE**: DDL, removes all rows, fast, cannot roll back (Oracle/MySQL). **DROP**: removes the whole table and its structure."},{"id":"cs-9571a673","d":"sql","t":"DDL and tables","f":"How do you copy a table's structure only (no rows) in Oracle?","b":"`CREATE TABLE copy AS SELECT * FROM t WHERE 1 = 0;` — the always-false condition copies the structure but no data. (Without it you get structure + data.)"},{"id":"cs-a6a9b4cc","d":"sql","t":"DDL and tables","f":"Add, modify, rename and drop a column in Oracle?","b":"`ALTER TABLE t ADD (c type)` · `ALTER TABLE t MODIFY (c type)` · `ALTER TABLE t RENAME COLUMN a TO b` · `ALTER TABLE t DROP COLUMN c`."},{"id":"cs-c171fbed","d":"sql","t":"DDL and tables","f":"Auto-numbering in Oracle vs MySQL vs SQL Server vs PostgreSQL?","b":"Oracle: `GENERATED ... AS IDENTITY` (12c+) or a `SEQUENCE`. MySQL: `AUTO_INCREMENT`. SQL Server: `IDENTITY(1,1)`. PostgreSQL: `SERIAL` / identity."},{"id":"cs-e804a90a","d":"sql","t":"DDL and tables","f":"Does DDL commit in Oracle? What are the consequences?","b":"Yes — every DDL statement **implicitly commits** (before and after). You cannot roll back a `CREATE`/`ALTER`/`DROP`/`TRUNCATE`, and it also commits any pending DML."},{"id":"cs-c8888fd1","d":"sql","t":"DDL and tables","f":"Why do gaps appear in identity/sequence numbers?","b":"Sequences hand out values independently of transactions. Rolled-back inserts, cached values and deleted rows never give the numbers back — gaps are normal."},{"id":"cs-3fa20995","d":"sql","t":"Constraints and keys","f":"PRIMARY KEY vs UNIQUE?","b":"PK = `NOT NULL` + `UNIQUE`, **one per table** (can be composite). UNIQUE allows many per table and (in Oracle) NULLs."},{"id":"cs-c25767d0","d":"sql","t":"Constraints and keys","f":"What is a FOREIGN KEY, and what do `ON DELETE CASCADE` and `ON DELETE SET NULL` do?","b":"A column that must match a parent's PK/UNIQUE value (or be NULL). CASCADE deletes child rows with the parent; SET NULL nulls the child's FK. Oracle has **no `ON UPDATE`**."},{"id":"cs-a47d9efd","d":"sql","t":"Constraints and keys","f":"Which error codes: duplicate key, NULL into NOT NULL, check failure, missing parent, child rows exist?","b":"`ORA-00001` (unique), `ORA-01400` (NULL), `ORA-02290` (check), `ORA-02291` (parent key not found), `ORA-02292` (child record found)."},{"id":"cs-bf6e0e96","d":"sql","t":"Constraints and keys","f":"Does a CHECK constraint reject a NULL value?","b":"**No.** A CHECK passes when the condition is TRUE *or UNKNOWN*, so NULL slips through. Add NOT NULL if NULL must be rejected."},{"id":"cs-4b96fbda","d":"sql","t":"Constraints and keys","f":"What is a candidate key, primary key, alternate key, superkey?","b":"**Superkey**: any column set that identifies a row. **Candidate key**: a *minimal* superkey. **Primary key**: the candidate you choose. **Alternate key**: the other candidates."},{"id":"cs-54ce1fd5","d":"sql","t":"Constraints and keys","f":"Is DEFAULT a constraint? When is it applied?","b":"Not a constraint (it cannot be named). It is used only when the column is **omitted** from an INSERT (or DEFAULT is written). An explicit NULL stays NULL."},{"id":"cs-a308c610","d":"sql","t":"Constraints and keys","f":"Composite key — example?","b":"A key made of several columns, e.g. `PRIMARY KEY (order_id, product_id)` in an order-lines table: neither column is unique alone, the pair is."},{"id":"cs-1acec2e5","d":"sql","t":"SELECT and filtering","f":"Logical order of execution of a SELECT?","b":"`FROM/JOIN` → `WHERE` → `GROUP BY` → `HAVING` → `SELECT` → `DISTINCT` → `ORDER BY` → `FETCH/LIMIT`."},{"id":"cs-a903e6d2","d":"sql","t":"SELECT and filtering","f":"Why can't you use a column alias in `WHERE` in Oracle?","b":"`WHERE` runs **before** `SELECT`, so the alias does not exist yet (`ORA-00904`). Repeat the expression or use a subquery. Aliases *are* allowed in `ORDER BY`."},{"id":"cs-97252a5b","d":"sql","t":"SELECT and filtering","f":"`LIKE` wildcards? Which does Oracle *not* support?","b":"`%` = any run of characters, `_` = exactly one. Oracle does **not** support `[abc]`/`[^abc]` in LIKE — use `REGEXP_LIKE`."},{"id":"cs-32ca43a8","d":"sql","t":"SELECT and filtering","f":"`BETWEEN 10 AND 20` — inclusive?","b":"**Yes**, both ends are included. `x BETWEEN a AND b` = `x >= a AND x <= b`."},{"id":"cs-aad1cf4c","d":"sql","t":"SELECT and filtering","f":"Top-N rows in Oracle 12c+, pre-12c, MySQL, SQL Server?","b":"Oracle 12c+: `FETCH FIRST n ROWS ONLY`. Old Oracle: sort in a subquery, then `WHERE ROWNUM <= n`. MySQL/PostgreSQL: `LIMIT n`. SQL Server: `SELECT TOP n`."},{"id":"cs-1cd6d810","d":"sql","t":"SELECT and filtering","f":"The ROWNUM trap: `SELECT * FROM emp WHERE ROWNUM <= 3 ORDER BY salary DESC`?","b":"`ROWNUM` is assigned **before** `ORDER BY`, so this returns 3 *arbitrary* rows, then sorts them. Sort in a subquery first, then filter on ROWNUM (or use `FETCH FIRST`)."},{"id":"cs-f6978f2d","d":"sql","t":"SELECT and filtering","f":"Table alias syntax in Oracle?","b":"`FROM emp e` — **no `AS`** before a table alias (`AS` gives ORA-00933). `AS` is allowed for *column* aliases."},{"id":"cs-0e79f140","d":"sql","t":"SELECT and filtering","f":"What does `ORDER BY salary` do with NULLs in Oracle?","b":"NULLs are treated as the **largest** value: last in ASC, first in DESC. Override with `NULLS FIRST` / `NULLS LAST`."},{"id":"cs-773d3f1b","d":"sql","t":"DML and transactions","f":"What does `INSERT ALL` do?","b":"Oracle-only multi-row/multi-table insert: `INSERT ALL INTO t (a) VALUES (1) INTO t (a) VALUES (2) SELECT * FROM dual;`. (Multi-row `VALUES (..),(..)` only arrives in Oracle 23ai.)"},{"id":"cs-3b6ff83c","d":"sql","t":"DML and transactions","f":"`UPDATE t SET x = 1` with no WHERE?","b":"Updates **every row**. Same for `DELETE FROM t`. Run the equivalent `SELECT` first when unsure."},{"id":"cs-61b2d439","d":"sql","t":"DML and transactions","f":"`COMMIT`, `ROLLBACK`, `SAVEPOINT` / `ROLLBACK TO`?","b":"COMMIT makes changes permanent; ROLLBACK undoes the whole transaction; `SAVEPOINT s` marks a point and `ROLLBACK TO s` undoes only what came after it (the transaction stays open)."},{"id":"cs-231645c9","d":"sql","t":"DML and transactions","f":"How do you insert rows from another table?","b":"`INSERT INTO target (a, b) SELECT x, y FROM source WHERE ...;`"},{"id":"cs-2cf0543a","d":"sql","t":"DML and transactions","f":"UPDATE using a subquery — write the pattern.","b":"`UPDATE emp SET salary = (SELECT AVG(salary) FROM emp) WHERE emp_id = 5;` (a scalar subquery must return exactly one row)."},{"id":"cs-247123b1","d":"sql","t":"Aggregation and GROUP BY","f":"`COUNT(*)` vs `COUNT(col)` vs `COUNT(DISTINCT col)`?","b":"`COUNT(*)` counts rows; `COUNT(col)` counts non-NULL values; `COUNT(DISTINCT col)` counts distinct non-NULL values. `SUM/AVG/MIN/MAX` also ignore NULLs."},{"id":"cs-4d1c9e15","d":"sql","t":"Aggregation and GROUP BY","f":"`WHERE` vs `HAVING`?","b":"`WHERE` filters **rows before** grouping (no aggregates allowed, ORA-00934). `HAVING` filters **groups after** aggregation."},{"id":"cs-9b7a75bb","d":"sql","t":"Aggregation and GROUP BY","f":"The GROUP BY rule?","b":"Every non-aggregated column in SELECT must appear in GROUP BY (`ORA-00979` otherwise)."},{"id":"cs-632e4da1","d":"sql","t":"Aggregation and GROUP BY","f":"Find duplicate values in a column.","b":"`SELECT col, COUNT(*) FROM t GROUP BY col HAVING COUNT(*) > 1;`"},{"id":"cs-c5c75922","d":"sql","t":"Aggregation and GROUP BY","f":"What do `ROLLUP` and `CUBE` add?","b":"`ROLLUP(a, b)` → subtotals for (a,b), (a), and the grand total. `CUBE(a, b)` → all combinations (a,b), (a), (b), grand total. `GROUPING()` tells which rows are subtotals."},{"id":"cs-5d8afd8f","d":"sql","t":"Aggregation and GROUP BY","f":"How do you count rows per group including groups with zero rows?","b":"`LEFT JOIN` the child table and use `COUNT(child.key)` (not `COUNT(*)`, which would count the NULL-extended row as 1)."},{"id":"cs-57e12a19","d":"sql","t":"Joins and set operators","f":"INNER vs LEFT vs RIGHT vs FULL vs CROSS join?","b":"INNER = matches in both. LEFT = all left + matches. RIGHT = all right + matches. FULL = all of both. CROSS = every combination (m × n rows)."},{"id":"cs-f63e2f55","d":"sql","t":"Joins and set operators","f":"`ON` vs `WHERE` in an outer join?","b":"`ON` defines how rows are matched; `WHERE` filters after. A `WHERE` on the **optional** side turns a LEFT JOIN into an inner join."},{"id":"cs-37872435","d":"sql","t":"Joins and set operators","f":"Find rows in A with no match in B (anti-join).","b":"`SELECT a.* FROM a LEFT JOIN b ON a.id = b.id WHERE b.id IS NULL;` (or `NOT EXISTS`)."},{"id":"cs-723cc628","d":"sql","t":"Joins and set operators","f":"What is a self join? Example.","b":"A table joined to itself with two aliases, e.g. employee and manager: `FROM emp e JOIN emp m ON e.manager_id = m.emp_id`."},{"id":"cs-2fc50eb6","d":"sql","t":"Joins and set operators","f":"Old Oracle outer-join syntax?","b":"The `(+)` operator on the optional side: `WHERE e.dept_id = d.dept_id (+)` keeps all employees. Prefer ANSI `LEFT JOIN`."},{"id":"cs-fb93619e","d":"sql","t":"Joins and set operators","f":"`UNION` vs `UNION ALL` vs `INTERSECT` vs `MINUS`?","b":"`UNION` = combine and remove duplicates; `UNION ALL` = combine, keep duplicates (faster); `INTERSECT` = rows in both; `MINUS` (Oracle; `EXCEPT` elsewhere) = rows in the first but not the second."},{"id":"cs-740b22b7","d":"sql","t":"Joins and set operators","f":"Rules for set operators?","b":"Same number of columns with compatible types; column names come from the first query; a single `ORDER BY` goes at the very end."},{"id":"cs-1872968d","d":"sql","t":"Joins and set operators","f":"Which databases lack FULL OUTER JOIN?","b":"MySQL. Emulate: `LEFT JOIN ... UNION ... RIGHT JOIN`."},{"id":"cs-42cb391f","d":"sql","t":"Subqueries","f":"Scalar, column, row and table subquery?","b":"Scalar = one value; column = one column, many rows (use IN/ANY/ALL); row = one row, several columns; table (derived table) = in `FROM`."},{"id":"cs-9b7d6d75","d":"sql","t":"Subqueries","f":"What is a correlated subquery?","b":"An inner query that refers to the outer row, conceptually re-run for each outer row: `WHERE salary > (SELECT AVG(salary) FROM emp WHERE dept_id = e.dept_id)`."},{"id":"cs-d0f1442c","d":"sql","t":"Subqueries","f":"Why does `x NOT IN (subquery)` return nothing if the subquery contains a NULL?","b":"`x <> NULL` is UNKNOWN for every comparison, so the whole condition is never TRUE. Use `NOT EXISTS` (NULL-safe) instead."},{"id":"cs-b5538ff9","d":"sql","t":"Subqueries","f":"Explain `> ANY`, `> ALL`, `= ANY`, `!= ALL`.","b":"`> ANY` = greater than the **minimum**; `> ALL` = greater than the **maximum**; `= ANY` = `IN`; `!= ALL` = `NOT IN`. (`!= ANY` is **not** `NOT IN`.)"},{"id":"cs-c3211a41","d":"sql","t":"Subqueries","f":"`> ALL` / `> ANY` on an empty subquery?","b":"`ALL` over nothing is **TRUE**; `ANY` over nothing is **FALSE**."},{"id":"cs-2aed2059","d":"sql","t":"Subqueries","f":"Second highest salary — two ways.","b":"`SELECT MAX(salary) FROM emp WHERE salary < (SELECT MAX(salary) FROM emp);` or `DENSE_RANK() OVER (ORDER BY salary DESC) = 2` in a subquery."},{"id":"cs-b8a90c4a","d":"sql","t":"Subqueries","f":"`EXISTS` — what does it return and why is it efficient?","b":"TRUE if the subquery returns at least one row; it stops at the first match and ignores the selected columns (`SELECT 1` is conventional)."},{"id":"cs-b7014dc9","d":"sql","t":"Subqueries","f":"ORA-01427?","b":"\"single-row subquery returns more than one row\" — a scalar subquery (used with `=`, `>`…) returned several rows. Use `IN`/`ANY` or make it return one row."},{"id":"cs-7fd0a5dd","d":"sql","t":"Functions","f":"`SUBSTR(s, start, len)` — quirks?","b":"1-based; a **negative** start counts from the end; `SUBSTR('Oracle', -3)` = `'cle'`."},{"id":"cs-d5eb1fca","d":"sql","t":"Functions","f":"What does `INSTR` return when nothing is found?","b":"**0** (positions are 1-based). Signature: `INSTR(s, sub [, start [, occurrence]])`."},{"id":"cs-cfb1a285","d":"sql","t":"Functions","f":"`ROUND(1234.567, -2)`?","b":"`1200`. A negative second argument rounds to the left of the decimal point. `TRUNC` cuts instead of rounding."},{"id":"cs-2c4f8a45","d":"sql","t":"Functions","f":"Date arithmetic and useful functions in Oracle?","b":"`SYSDATE ± n` adds/subtracts **days**; `ADD_MONTHS(d, n)`; `MONTHS_BETWEEN(a, b)`; `LAST_DAY(d)`; `NEXT_DAY(d, 'MONDAY')`; `TRUNC(d, 'MONTH')`."},{"id":"cs-5be47a2a","d":"sql","t":"Functions","f":"`TO_CHAR(d, 'DD-Mon-YYYY')` vs `TO_DATE('01-Jan-2025', 'DD-Mon-YYYY')`?","b":"`TO_CHAR` converts a date/number to text with a format; `TO_DATE` parses text into a DATE. `FM` in a format removes padding."},{"id":"cs-7e782d02","d":"sql","t":"Functions","f":"`NVL`, `NVL2`, `NULLIF`, `COALESCE`?","b":"`NVL(a,b)` = b if a is NULL. `NVL2(x,a,b)` = a if x is not NULL else b. `NULLIF(a,b)` = NULL if a = b else a. `COALESCE(a,b,c…)` = first non-NULL (standard SQL)."},{"id":"cs-3cb3a39c","d":"sql","t":"Functions","f":"How do you compute someone's age in whole years in Oracle?","b":"`TRUNC(MONTHS_BETWEEN(SYSDATE, dob) / 12)`."},{"id":"cs-e13b3ddb","d":"sql","t":"Functions","f":"Oracle's regular-expression functions?","b":"`REGEXP_LIKE` (condition), `REGEXP_SUBSTR`, `REGEXP_REPLACE`, `REGEXP_INSTR`, `REGEXP_COUNT`. Example: `WHERE REGEXP_LIKE(name, '^[ABC]')`."},{"id":"cs-1ee282f8","d":"sql","t":"Views and indexes","f":"What is a view and does it store data?","b":"A stored `SELECT` (a virtual table). It stores **no data** (unlike a materialized view); it runs the query each time it is used."},{"id":"cs-ecb2f94f","d":"sql","t":"Views and indexes","f":"What does `WITH CHECK OPTION` do?","b":"Rejects INSERT/UPDATE through the view that would produce rows the view itself could not see (`ORA-01402`). `WITH READ ONLY` blocks all DML."},{"id":"cs-10eb4120","d":"sql","t":"Views and indexes","f":"When is a view not updatable?","b":"When it contains joins (in general), `DISTINCT`, `GROUP BY`, aggregates, set operators or `ROWNUM`. An `INSTEAD OF` trigger can make it updatable."},{"id":"cs-02edbd49","d":"sql","t":"Views and indexes","f":"What data structure does a normal index use, and what does it speed up?","b":"A **B-tree**: lookups, range scans, and sorted access in O(log n) instead of a full table scan. It slows writes and uses space."},{"id":"cs-12187419","d":"sql","t":"Views and indexes","f":"Composite index rule?","b":"Leftmost-prefix: an index on `(a, b)` helps filters on `a` or `a AND b`, **not** on `b` alone (except an occasional skip scan)."},{"id":"cs-9f9155c6","d":"sql","t":"Views and indexes","f":"Name four reasons an index is not used.","b":"A function/expression on the indexed column; a leading wildcard `LIKE '%x'`; implicit type conversion; a non-selective filter or tiny table (full scan is cheaper); `IS NULL` on a single-column B-tree (NULLs aren't stored)."},{"id":"cs-af103543","d":"sql","t":"Views and indexes","f":"Which indexes does Oracle create automatically?","b":"One for each `PRIMARY KEY` and `UNIQUE` constraint. It does **not** index foreign keys — do it yourself."},{"id":"cs-ff23adcb","d":"sql","t":"Window functions","f":"`ROW_NUMBER` vs `RANK` vs `DENSE_RANK` on 100, 90, 90, 80?","b":"ROW_NUMBER: 1,2,3,4 · RANK: 1,2,2,**4** (gap) · DENSE_RANK: 1,2,2,**3** (no gap)."},{"id":"cs-9f63f558","d":"sql","t":"Window functions","f":"`GROUP BY` vs `PARTITION BY`?","b":"GROUP BY collapses each group to **one row**. PARTITION BY defines the window but **keeps every row**, adding the calculation as a column."},{"id":"cs-ef9d7552","d":"sql","t":"Window functions","f":"Why can't you filter on a window function in `WHERE`?","b":"Window functions run after WHERE (in the SELECT step). Compute in a subquery/CTE, then filter outside."},{"id":"cs-62db6948","d":"sql","t":"Window functions","f":"Running total pattern?","b":"`SUM(amount) OVER (ORDER BY day ROWS UNBOUNDED PRECEDING)`. Write the frame explicitly — the default `RANGE` frame treats ties as one."},{"id":"cs-3d5df65f","d":"sql","t":"Window functions","f":"What do `LAG` and `LEAD` do?","b":"Access the previous / next row's value within the window: `LAG(amount) OVER (ORDER BY month)`; the first row's `LAG` is NULL (or a default you pass)."},{"id":"cs-9988d251","d":"sql","t":"Window functions","f":"Top-N per group pattern.","b":"`SELECT * FROM (SELECT ..., DENSE_RANK() OVER (PARTITION BY grp ORDER BY score DESC) rnk FROM t) WHERE rnk <= N;`"},{"id":"cs-533724df","d":"sql","t":"Window functions","f":"The `LAST_VALUE` trap?","b":"With an `ORDER BY`, the default frame ends at the **current row**, so `LAST_VALUE` returns the current row. Use `ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING`."},{"id":"cs-7aeb4b19","d":"sql","t":"PL/SQL basics","f":"Structure of an anonymous PL/SQL block?","b":"`DECLARE` (optional) → `BEGIN` (mandatory) → `EXCEPTION` (optional) → `END;` followed by `/` in SQL*Plus/Developer to run it."},{"id":"cs-af45c1da","d":"sql","t":"PL/SQL basics","f":"How do you print output from PL/SQL?","b":"`SET SERVEROUTPUT ON;` then `DBMS_OUTPUT.PUT_LINE('text');`."},{"id":"cs-52b17228","d":"sql","t":"PL/SQL basics","f":"Assignment vs comparison operators in PL/SQL?","b":"Assignment is `:=`; comparison is `=` (`!=`, `<>`, `<`, `>`…)."},{"id":"cs-ea2a72d3","d":"sql","t":"PL/SQL basics","f":"`%TYPE` vs `%ROWTYPE`?","b":"`v emp.salary%TYPE` — a variable with the same type as one column. `r emp%ROWTYPE` — a record with a field for every column of the row. Both adapt automatically if the table changes."},{"id":"cs-81a9b14e","d":"sql","t":"PL/SQL basics","f":"What must `SELECT ... INTO` return, and what happens otherwise?","b":"**Exactly one row**. Zero rows → `NO_DATA_FOUND`; more than one → `TOO_MANY_ROWS`."},{"id":"cs-c49d1679","d":"sql","t":"PL/SQL basics","f":"`&` vs `:` in Oracle tools?","b":"`&x` is a **substitution variable** (SQL*Plus/Developer replaces it with typed text before running); `:x` is a **bind variable** (a real placeholder value)."},{"id":"cs-9a86bfd3","d":"sql","t":"PL/SQL basics","f":"Loop forms in PL/SQL?","b":"`LOOP ... EXIT WHEN cond; END LOOP;` · `WHILE cond LOOP ... END LOOP;` · `FOR i IN [REVERSE] a..b LOOP ... END LOOP;` (the counter is implicit and read-only)."},{"id":"cs-7751e710","d":"sql","t":"PL/SQL basics","f":"`IF` syntax gotcha?","b":"It is `ELSIF` (not `ELSEIF`) and every `IF` ends with `END IF;`. A NULL condition is UNKNOWN and falls to the `ELSE` branch."},{"id":"cs-7973372e","d":"sql","t":"Procedures, functions, packages","f":"Procedure vs function?","b":"A procedure performs an action and returns values only through `OUT` parameters; a function **must `RETURN` one value** and can be used inside SQL expressions."},{"id":"cs-e6707394","d":"sql","t":"Procedures, functions, packages","f":"Parameter modes?","b":"`IN` (default, read-only), `OUT` (starts NULL, assigned inside), `IN OUT` (read and modified). Parameter types have **no length** (`VARCHAR2`, not `VARCHAR2(50)`)."},{"id":"cs-2d564330","d":"sql","t":"Procedures, functions, packages","f":"How do you call a procedure and a function?","b":"Procedure: `EXEC p(x)` or `BEGIN p(x); END;`. Function: as an expression `v := f(x);` or `SELECT f(x) FROM dual;`. Named notation uses `=>`."},{"id":"cs-b8c7fc01","d":"sql","t":"Procedures, functions, packages","f":"Parameter notation styles?","b":"Positional `p(1, 2)`, named `p(b => 2, a => 1)`, mixed (positional first, then named)."},{"id":"cs-1241d5af","d":"sql","t":"Procedures, functions, packages","f":"What is a package, and why use one?","b":"A specification (public declarations) plus a body (implementation and private items). Groups related code, hides internals, keeps state per session and loads into memory once."},{"id":"cs-42d81d6d","d":"sql","t":"Procedures, functions, packages","f":"Procedure header syntax gotcha?","b":"After `CREATE OR REPLACE PROCEDURE p (...) IS|AS` there is **no `DECLARE`**; local variables go between `IS` and `BEGIN`."},{"id":"cs-b7e7abc9","d":"sql","t":"Cursors and records","f":"Implicit vs explicit cursor?","b":"Implicit: Oracle opens one for every DML/`SELECT INTO`, accessed as `SQL%...`. Explicit: you declare it and control `OPEN` → `FETCH` → `CLOSE`."},{"id":"cs-57358191","d":"sql","t":"Cursors and records","f":"Cursor attributes?","b":"`%FOUND`, `%NOTFOUND`, `%ROWCOUNT`, `%ISOPEN`. For implicit cursors `SQL%ISOPEN` is always FALSE (Oracle closes it immediately)."},{"id":"cs-efa55312","d":"sql","t":"Cursors and records","f":"Easiest way to loop over a query?","b":"`FOR rec IN (SELECT ...) LOOP ... END LOOP;` — opens, fetches, exits and closes automatically."},{"id":"cs-da15e325","d":"sql","t":"Cursors and records","f":"`SQL%ROWCOUNT` after an UPDATE?","b":"The number of rows the UPDATE changed — the standard way to detect that nothing matched."},{"id":"cs-8f3d3485","d":"sql","t":"Cursors and records","f":"Declare a record type.","b":"`TYPE emp_rec IS RECORD (name VARCHAR2(30), sal NUMBER);` then `v emp_rec;` — the type must be declared before the variable."},{"id":"cs-b5e6ed1a","d":"sql","t":"Triggers","f":"What are `:OLD` and `:NEW`, and what do they need?","b":"Row values before/after the change. They exist only in **row-level** triggers (`FOR EACH ROW`), otherwise `ORA-04082`. On INSERT `:OLD` is NULL; on DELETE `:NEW` is NULL."},{"id":"cs-081e5424","d":"sql","t":"Triggers","f":"Row-level vs statement-level trigger?","b":"Row-level fires once per affected row (`FOR EACH ROW`); statement-level fires once per statement, even for zero rows."},{"id":"cs-ad24dc31","d":"sql","t":"Triggers","f":"Firing order?","b":"BEFORE statement → (BEFORE row → the change → AFTER row) for each row → AFTER statement."},{"id":"cs-b4a25724","d":"sql","t":"Triggers","f":"How does a trigger reject a change?","b":"`RAISE_APPLICATION_ERROR(-20001, 'message');` — aborts and rolls back the triggering statement."},{"id":"cs-057f5b88","d":"sql","t":"Triggers","f":"What is the mutating-table error and how do you avoid it?","b":"`ORA-04091`: a row trigger reads or changes the very table that is being modified. Use a **compound trigger**, move logic to statement level, or redesign."},{"id":"cs-1545564e","d":"sql","t":"Triggers","f":"What is an `INSTEAD OF` trigger for?","b":"Making a complex (non-updatable) **view** accept DML: the trigger performs the underlying inserts/updates."},{"id":"cs-a52ffc20","d":"sql","t":"Triggers","f":"Can a trigger `COMMIT`?","b":"No (`ORA-04092`) unless it is an autonomous transaction. The trigger's work belongs to the triggering transaction."},{"id":"cs-1b9d6ebe","d":"sql","t":"CTEs and recursion","f":"What is a CTE and is it faster than a subquery?","b":"A named temporary result (`WITH x AS (...)`) valid for one statement. Mostly about **readability**; the optimizer usually treats it like the equivalent subquery."},{"id":"cs-b12b2c6c","d":"sql","t":"CTEs and recursion","f":"Anatomy of a recursive CTE?","b":"**Anchor member** `UNION ALL` **recursive member** that refers to the CTE, plus a stop condition. Oracle omits the `RECURSIVE` keyword and needs a column list; PostgreSQL/MySQL write `WITH RECURSIVE`."},{"id":"cs-d17f6785","d":"sql","t":"CTEs and recursion","f":"Explain `CONNECT BY PRIOR emp_id = manager_id START WITH manager_id IS NULL`.","b":"Oracle hierarchical query. `START WITH` picks the roots; `PRIOR` marks the **parent** side of the link, so each child's `manager_id` equals its parent's `emp_id`. `LEVEL` gives the depth."},{"id":"cs-bbbec7ea","d":"sql","t":"CTEs and recursion","f":"Generate the numbers 1 to 10 without a table in Oracle.","b":"`SELECT LEVEL FROM dual CONNECT BY LEVEL <= 10;`"},{"id":"cs-b2011e5e","d":"sql","t":"CTEs and recursion","f":"How do you stop a recursive query from looping on cyclic data?","b":"A depth guard (`WHERE lvl < 20`), `NOCYCLE`/`CYCLE` clause, `MAXRECURSION` (SQL Server) — otherwise ORA-32044 / ORA-01436."},{"id":"cs-d1595843","d":"sql","t":"Transactions and isolation","f":"What does ACID stand for?","b":"**A**tomicity (all or nothing), **C**onsistency (valid state → valid state), **I**solation (concurrent transactions don't interfere), **D**urability (committed data survives crashes)."},{"id":"cs-94f8d748","d":"sql","t":"Transactions and isolation","f":"Dirty, non-repeatable and phantom reads?","b":"Dirty = reading another transaction's **uncommitted** change. Non-repeatable = the same **row** changes between two reads (committed update). Phantom = the same **query** returns different **rows** (committed insert/delete)."},{"id":"cs-d15ea74c","d":"sql","t":"Transactions and isolation","f":"The four isolation levels and what each prevents?","b":"Read Uncommitted (nothing) → Read Committed (dirty reads) → Repeatable Read (+ non-repeatable reads) → Serializable (+ phantoms)."},{"id":"cs-cd2cbf5c","d":"sql","t":"Transactions and isolation","f":"Oracle's default isolation level? Dirty reads?","b":"`READ COMMITTED`; Oracle **never** allows dirty reads (it supports only Read Committed, Serializable and Read Only) and readers never block writers thanks to undo-based read consistency (MVCC)."},{"id":"cs-9399329c","d":"sql","t":"Transactions and isolation","f":"What does `SELECT ... FOR UPDATE SKIP LOCKED` do?","b":"Locks the selected rows but **skips** rows other sessions have locked — ideal for several workers taking different jobs from a queue table."},{"id":"cs-b39eae54","d":"sql","t":"Transactions and isolation","f":"What is a deadlock and how do you prevent it?","b":"Two transactions each wait for a lock the other holds (`ORA-00060`). Prevent it by locking in a **consistent order**, keeping transactions short, and retrying."},{"id":"cs-bfaeab5f","d":"sql","t":"Transactions and isolation","f":"Optimistic vs pessimistic locking?","b":"Pessimistic locks first (`FOR UPDATE`); optimistic doesn't lock but checks a **version column** at update time (`WHERE id = :id AND version = :v`) and retries on conflict."},{"id":"cs-216d0a80","d":"sql","t":"Normalization and design","f":"State 1NF, 2NF and 3NF.","b":"1NF: atomic values, no repeating groups. 2NF: 1NF + no **partial** dependency (non-key column depending on part of a composite key). 3NF: 2NF + no **transitive** dependency (non-key → non-key). \"The key, the whole key and nothing but the key.\""},{"id":"cs-825ef1ec","d":"sql","t":"Normalization and design","f":"BCNF in one sentence?","b":"For every functional dependency X → Y, **X must be a superkey**."},{"id":"cs-ac829139","d":"sql","t":"Normalization and design","f":"The three anomalies?","b":"**Update** (inconsistent copies), **insert** (can't record a fact without another), **delete** (removing a row loses an unrelated fact)."},{"id":"cs-b7275185","d":"sql","t":"Normalization and design","f":"How do you model many-to-many?","b":"A **junction (bridge) table** holding both foreign keys (the pair is the primary key); attributes of the relationship (e.g. grade) live there."},{"id":"cs-0d2fc973","d":"sql","t":"Normalization and design","f":"Natural vs surrogate key?","b":"Natural = real-world value (email); can change. Surrogate = generated number; never changes. Common practice: surrogate PK + a `UNIQUE` constraint on the natural key."},{"id":"cs-6debb178","d":"sql","t":"Normalization and design","f":"What is denormalization and when is it acceptable?","b":"Deliberately storing redundant data to speed reads (reporting, snapshots such as price-at-purchase). Do it **after** normalizing, for a measured problem."},{"id":"cs-513b1a97","d":"sql","t":"Normalization and design","f":"OLTP vs OLAP schema style?","b":"OLTP: many small transactions, **normalized** (3NF). OLAP: large read-only aggregations, **denormalized star schema** (fact table + dimension tables)."},{"id":"cs-062d88f3","d":"sql","t":"Query tuning","f":"How do you see a query's execution plan in Oracle?","b":"`EXPLAIN PLAN FOR <query>;` then `SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY);` — or F10 in SQL Developer, or `AUTOTRACE`."},{"id":"cs-4b321e44","d":"sql","t":"Query tuning","f":"How do you read a plan?","b":"It is a tree by indentation: the **most indented** operation runs first, feeding its parent; siblings run top to bottom. Compare estimated vs actual rows."},{"id":"cs-b9ca5dde","d":"sql","t":"Query tuning","f":"What are optimizer statistics and what happens when they are stale?","b":"Data-distribution numbers (row counts, distinct values, histograms) the cost-based optimizer uses to estimate cost. Stale stats → wrong estimates → bad plans. Refresh with `DBMS_STATS.GATHER_TABLE_STATS`."},{"id":"cs-37889ee7","d":"sql","t":"Query tuning","f":"Nested loops vs hash join vs sort-merge?","b":"Nested loops: small outer set + indexed inner lookups. Hash: big unsorted inputs, **equality** joins. Sort-merge: pre-sorted inputs or non-equality joins."},{"id":"cs-10214e12","d":"sql","t":"Query tuning","f":"What does SARGable mean?","b":"A predicate written so an index can be used: no function/arithmetic on the indexed column, no leading `%`, no implicit type conversion."},{"id":"cs-a3b1adc1","d":"sql","t":"Query tuning","f":"Why use bind variables?","b":"Identical statement text → **soft parse** (plan reuse) instead of a hard parse each time, and protection against SQL injection."},{"id":"cs-b8d49ade","d":"sql","t":"Query tuning","f":"Set-based vs row-by-row?","b":"One SQL statement handling many rows beats a PL/SQL loop running one statement per row (avoids thousands of context switches). Use `FORALL`/`BULK COLLECT` if you must loop."},{"id":"cs-cb3784b9","d":"sql","t":"PL/SQL exceptions","f":"Structure of exception handling?","b":"`EXCEPTION WHEN name THEN ... WHEN OTHERS THEN ...` — the handler runs, then the **block ends** (execution doesn't resume after the failing statement). `OTHERS` must be last."},{"id":"cs-203d4ca3","d":"sql","t":"PL/SQL exceptions","f":"Name five predefined exceptions.","b":"`NO_DATA_FOUND`, `TOO_MANY_ROWS`, `ZERO_DIVIDE`, `DUP_VAL_ON_INDEX`, `VALUE_ERROR` (also `INVALID_NUMBER`, `CURSOR_ALREADY_OPEN`, `CASE_NOT_FOUND`)."},{"id":"cs-6737dc01","d":"sql","t":"PL/SQL exceptions","f":"`RAISE` vs `RAISE_APPLICATION_ERROR`?","b":"`RAISE name;` throws a named exception. `RAISE_APPLICATION_ERROR(-20000…-20999, 'msg')` reports your own error number and message to the caller; in a trigger it aborts the DML."},{"id":"cs-5a28a1a8","d":"sql","t":"PL/SQL exceptions","f":"What is `PRAGMA EXCEPTION_INIT`?","b":"A compiler directive that binds a name to an Oracle error number, e.g. `PRAGMA EXCEPTION_INIT(child_exists, -2292);` so you can catch it by name."},{"id":"cs-e3dd1478","d":"sql","t":"PL/SQL exceptions","f":"What do `SQLCODE` and `SQLERRM` return?","b":"The current error's number (negative; `+100` for NO_DATA_FOUND; `+1` for user-defined) and message text. Copy them into variables before using them in SQL."},{"id":"cs-b336c2ba","d":"sql","t":"PL/SQL exceptions","f":"Why is `WHEN OTHERS THEN NULL` dangerous?","b":"It silently swallows every error, including real bugs. Use it only as a last handler that **logs and re-raises** (`RAISE;`)."},{"id":"cs-212377f1","d":"sql","t":"PL/SQL exceptions","f":"What if an exception occurs in the DECLARE section?","b":"The same block's handler does **not** catch it — it propagates to the enclosing block."},{"id":"cs-b8edd6c1","d":"sql","t":"PL/SQL exceptions","f":"How do you keep processing after one bad row in a loop?","b":"Wrap each iteration in its own `BEGIN ... EXCEPTION ... END;` sub-block so its handler swallows the failure locally."},{"id":"cs-3a32a358","d":"sql","t":"PL/SQL exceptions","f":"Does a handled exception roll back earlier changes?","b":"**No.** Only an unhandled exception rolls back the failed call. After handling, `ROLLBACK` (or `ROLLBACK TO savepoint`) yourself if needed."},{"id":"ch-ae5c45b7","d":"heap","t":"Heap basics","f":"What does a heap guarantee — and what does it not?","b":"Only that the **root** is the minimum (min-heap) or maximum (max-heap): every parent is `<=` (or `>=`) its children. The rest is **not sorted**."},{"id":"ch-095a8f6d","d":"heap","t":"Heap basics","f":"Heap operation complexities: peek, push, pop, heapify?","b":"Peek **O(1)** · push **O(log n)** (sift up) · pop **O(log n)** (sift down) · heapify a whole array **O(n)**."},{"id":"ch-3c010e06","d":"heap","t":"Heap basics","f":"Why is `heapify` O(n) rather than O(n log n)?","b":"Half the nodes are leaves (0 work), a quarter can sink 1 level, an eighth 2 levels… the sum `n·Σk/2^(k+1)` converges to O(n). Only the few nodes near the top can sink far."},{"id":"ch-edeecbf0","d":"heap","t":"Heap basics","f":"Array indices of parent / left / right child (0-based)?","b":"parent `(i - 1) // 2` · left `2i + 1` · right `2i + 2`. A heap is a *complete* binary tree stored level by level in an array."},{"id":"ch-c3d9c62d","d":"heap","t":"Heap basics","f":"How does `pop` work internally?","b":"Move the **last** item to the root, then **sift down**, each time swapping with the **smaller** child (min-heap) until the heap property holds."},{"id":"ch-c6342cd8","d":"heap","t":"Heap basics","f":"Python's `heapq` — min or max? How do you get a max-heap?","b":"`heapq` is a **min-heap**. For a max-heap push **negated** values and negate again when reading (`-heap[0]`). Python 3.14 adds `*_max` functions."},{"id":"ch-b41c27f0","d":"heap","t":"Heap basics","f":"`heapq.heappushpop(h, x)` vs `heapq.heapreplace(h, x)`?","b":"`heappushpop`: push `x` then pop the smallest (faster than separate calls). `heapreplace`: pop the smallest **then** push `x` (heap must be non-empty)."},{"id":"ch-7b6dfb5c","d":"heap","t":"Heap basics","f":"Two `heapq` tuple pitfalls?","b":"(1) Ties fall through to the next tuple element — if that is not comparable (e.g. a `ListNode`) you get `TypeError`: add a unique tie-breaker `(key, i, obj)`. (2) Negate the sort **key**, not the whole tuple, for a max-heap."},{"id":"ch-facd1c1b","d":"heap","t":"Heap basics","f":"What is an *invariant* and why does it matter for heap problems?","b":"A condition kept true throughout, describing **what the heap currently contains** (e.g. \"the min-heap holds the K largest seen so far\"). It tells you when to push, pop or ignore."},{"id":"ch-78aaf288","d":"heap","t":"Heap basics","f":"When is a heap the right tool?","b":"When the set **keeps changing** while you repeatedly need its min/max. If you need the sorted order only once, just sort."},{"id":"ch-8435427e","d":"heap","t":"Pattern 1 — Top K","f":"Recognition signal for Top K?","b":"\"I only care about the best **K** elements, not the whole order.\""},{"id":"ch-9b99ccfc","d":"heap","t":"Pattern 1 — Top K","f":"K largest elements — which heap and why?","b":"A **min-heap of size K**. Its root is the *weakest* of the K largest — the one to evict when a bigger element arrives."},{"id":"ch-e44fda8e","d":"heap","t":"Pattern 1 — Top K","f":"K smallest elements — which heap?","b":"A **max-heap of size K** (negated in Python): the root is the largest of the K smallest = the weakest candidate."},{"id":"ch-a5f64c81","d":"heap","t":"Pattern 1 — Top K","f":"The general rule for the heap direction in Top K?","b":"Ask \"**which of my K candidates is the weakest?**\" — that must be at the root. The direction follows from what you evict."},{"id":"ch-8d2453d4","d":"heap","t":"Pattern 1 — Top K","f":"Complexity of Top K with a size-K heap, and versus sorting?","b":"**O(n log k)** time, **O(k)** space (each heap op costs log k because the heap only holds K). Sorting is O(n log n). Quickselect is O(n) average for static arrays."},{"id":"ch-006a9a5f","d":"heap","t":"Pattern 1 — Top K","f":"Kth largest — write the core loop.","b":"```\nfor x in nums:\n    heapq.heappush(heap, x)\n    if len(heap) > k:\n        heapq.heappop(heap)\nreturn heap[0]\n```"},{"id":"ch-78c279a6","d":"heap","t":"Pattern 1 — Top K","f":"Top K Frequent Elements — what changes compared with Top K values?","b":"The **score** is the frequency: build `Counter`, then keep a min-heap of `(count, element)` of size K."},{"id":"ch-87576886","d":"heap","t":"Pattern 1 — Top K","f":"K Closest Points — heap, key and shortcut?","b":"**Max-heap** of size K on distance (weakest = farthest). Use `x*x + y*y` — no `sqrt` needed since it preserves order."},{"id":"ch-60b56f42","d":"heap","t":"Pattern 1 — Top K","f":"K Smallest Pairs / Kth in a sorted matrix — which pattern is hiding here?","b":"**K-way merge**: each row is a sorted stream. Push the first element of each row, pop the smallest and push its row's next element."},{"id":"ch-df4d6684","d":"heap","t":"Pattern 2 — Two heaps","f":"Recognition signal for two heaps?","b":"A **dynamic median** — a boundary between the smaller half and the larger half that keeps moving."},{"id":"ch-5f9102e5","d":"heap","t":"Pattern 2 — Two heaps","f":"Which heap holds which half?","b":"**Max-heap** = smaller half (its root is the largest of the small side); **min-heap** = larger half (its root is the smallest of the large side)."},{"id":"ch-bb46c6ec","d":"heap","t":"Pattern 2 — Two heaps","f":"The size invariant of the two heaps?","b":"`len(max) == len(min)` or `len(max) == len(min) + 1`. (Which side holds the extra is a convention; the real rule is \"differ by at most 1\".)"},{"id":"ch-65e1e8a7","d":"heap","t":"Pattern 2 — Two heaps","f":"How do you insert x and rebalance?","b":"If `x <= max-heap top` → max-heap, else min-heap. Then if max > min + 1 move max's top to min; if min > max move min's top to max."},{"id":"ch-c28d1d49","d":"heap","t":"Pattern 2 — Two heaps","f":"How do you read the median?","b":"Odd count: root of the bigger (max) heap. Even count: average of the two roots. Insert O(log n), median O(1)."},{"id":"ch-42ce97c8","d":"heap","t":"Pattern 2 — Two heaps","f":"Sliding Window Median — which two patterns combine?","b":"**Two heaps + lazy deletion**: elements leave the window, so record deletions (by value) and prune stale tops, tracking the *valid* size of each heap separately."},{"id":"ch-5076f99e","d":"heap","t":"Pattern 3 — Repeatedly process min/max","f":"Recognition signal for pattern 3?","b":"\"At every step I need the **current** min/max, then I **modify the set** and repeat.\""},{"id":"ch-c4862c44","d":"heap","t":"Pattern 3 — Repeatedly process min/max","f":"How does pattern 3 differ from Top K?","b":"Top K keeps a *bounded* set of candidates; pattern 3 keeps **all remaining items** in the heap, consumes the best one(s) and pushes results back."},{"id":"ch-4efce293","d":"heap","t":"Pattern 3 — Repeatedly process min/max","f":"Connect Ropes — which two ropes should you merge and why?","b":"The **two smallest**. A merged rope's length is counted again in every later merge it joins, so small ropes should be counted more often and large ropes fewer times (Huffman coding)."},{"id":"ch-5a69f52b","d":"heap","t":"Pattern 3 — Repeatedly process min/max","f":"How many merges to connect n ropes into one?","b":"**n − 1** — each merge turns 2 ropes into 1."},{"id":"ch-a1de1c48","d":"heap","t":"Pattern 3 — Repeatedly process min/max","f":"Last Stone Weight — heap and invariant?","b":"**Max-heap**; invariant: it contains exactly the stones that remain. Pop two, push back `x - y` if non-zero."},{"id":"ch-987561c2","d":"heap","t":"Pattern 3 — Repeatedly process min/max","f":"Why do you extract *two* elements each step in Connect Ropes?","b":"A connection always involves two ropes: you need the smallest **and** the second smallest."},{"id":"ch-1483ed9a","d":"heap","t":"Pattern 3 — Repeatedly process min/max","f":"Min Cost to Hire K Workers — key transformation?","b":"Sort by `wage/quality`; the current worker's ratio is the group's rate. Keep the **K smallest qualities** so far in a **max-heap**; cost = ratio × sum of qualities."},{"id":"ch-60896559","d":"heap","t":"Pattern 3 — Repeatedly process min/max","f":"Furthest Building You Can Reach — heap and idea?","b":"**Min-heap of size = ladders** holding the largest climbs (they get ladders). When it overflows, the smallest climb is paid with bricks; stop when bricks < 0. Ladders belong on the biggest climbs."},{"id":"ch-4c6d6258","d":"heap","t":"Pattern 4 — K-way merge","f":"Recognition signal for K-way merge?","b":"**K sorted** sequences and you need the next smallest across them (or the merged result)."},{"id":"ch-3ce3391f","d":"heap","t":"Pattern 4 — K-way merge","f":"What goes into the heap in a K-way merge?","b":"**One candidate per source**: `(value, source_id, index)`. Pop the smallest, output it, push that source's next element."},{"id":"ch-c1fc3508","d":"heap","t":"Pattern 4 — K-way merge","f":"Complexity of merging K sorted lists with N total elements?","b":"**O(N log K)** time, O(K) extra space — better than O(N log N) because the heap holds at most K entries."},{"id":"ch-1e5f2a5e","d":"heap","t":"Pattern 4 — K-way merge","f":"Why does Merge K Sorted Linked Lists need an index in the heap tuple?","b":"`(node.val, i, node)` — with equal values Python compares the next element, and `ListNode` is not comparable → `TypeError`. The index is a tie-breaker."},{"id":"ch-20b9982a","d":"heap","t":"Pattern 4 — K-way merge","f":"Smallest Range Covering K Lists — what is the only move that can improve the range?","b":"Advance the list that holds the **current minimum** (the heap root); track the current maximum separately. Stop when any list is exhausted."},{"id":"ch-d7839f6c","d":"heap","t":"Pattern 4 — K-way merge","f":"100 huge sorted files → one sorted stream without loading them. What do you keep?","b":"A min-heap of the next unread item from each file (`(timestamp, file_id)`): O(K) memory."},{"id":"ch-2beb1742","d":"heap","t":"Pattern 5 — Sort + heap","f":"Recognition signal for sort + heap?","b":"Events arrive in some order and you must manage a **changing active set** (rooms, machines, CPUs)."},{"id":"ch-90a85a4e","d":"heap","t":"Pattern 5 — Sort + heap","f":"Meeting Rooms II — sort by what, heap of what?","b":"Sort by **start time**; min-heap of the **end times** of occupied rooms. If `heap[0] <= start` reuse that room (pop); push this meeting's end."},{"id":"ch-e87af0d8","d":"heap","t":"Pattern 5 — Sort + heap","f":"Why a *min*-heap of end times in Meeting Rooms II?","b":"Check the room that becomes free **earliest**; if even that one is not free, no room is."},{"id":"ch-f6743d4e","d":"heap","t":"Pattern 5 — Sort + heap","f":"Why sort by start (not end) in Meeting Rooms II?","b":"Because the decision \"is a room free?\" is made at each meeting's **start**. Sorting by end suits other problems (e.g. max non-overlapping meetings)."},{"id":"ch-c8f063be","d":"heap","t":"Pattern 5 — Sort + heap","f":"Boundary detail in interval problems: `<` or `<=`?","b":"A meeting ending at 10 and one starting at 10 don't overlap → free the room when `end <= start`."},{"id":"ch-424e82ce","d":"heap","t":"Pattern 5 — Sort + heap","f":"Single-Threaded CPU — what is sorted and what is heaped?","b":"Sort tasks by **start time** (arrival); min-heap of `(duration, id)` of the arrived tasks picks the next to run. If the heap is empty, jump time to the next arrival."},{"id":"ch-db5ab673","d":"heap","t":"Pattern 6 — Heap + greedy","f":"Recognition signal for heap + greedy?","b":"Choices **become feasible over time** (or as your state improves) and you always take the best feasible one."},{"id":"ch-40ae44c3","d":"heap","t":"Pattern 6 — Heap + greedy","f":"How does heap + greedy differ from Top K?","b":"In Top K all candidates exist from the start; here candidates **enter the pool dynamically** and earlier choices change what becomes available."},{"id":"ch-52c9d4ec","d":"heap","t":"Pattern 6 — Heap + greedy","f":"IPO — the algorithm?","b":"Sort projects by capital; while capital grows push every affordable project's profit into a **max-heap**; take the max profit; repeat up to K times."},{"id":"ch-d93fa8e8","d":"heap","t":"Pattern 6 — Heap + greedy","f":"Why is the IPO greedy choice safe?","b":"Capital is a **threshold, not a cost**: taking the highest-profit affordable project leaves you with at least as much capital as any alternative, so it can never shrink the future choices."},{"id":"ch-2485bd68","d":"heap","t":"Pattern 6 — Heap + greedy","f":"Maximum Performance of a Team — transformation?","b":"Sort by efficiency **descending**; the current efficiency is the team's minimum. Keep the **K largest speeds** seen so far (min-heap) and the running sum."},{"id":"ch-af12fd07","d":"heap","t":"Pattern 6 — Heap + greedy","f":"Minimum Refueling Stops — when do you refuel?","b":"**Delay** the decision: push every passed station's fuel into a max-heap; only when you run out, pop the largest and count a stop."},{"id":"ch-fb345ed4","d":"heap","t":"Pattern 7 — Lazy deletion","f":"What is lazy deletion?","b":"Don't remove an arbitrary element from a heap (O(n)). Leave stale entries inside and **discard them only when they reach the top**, after checking validity."},{"id":"ch-4f06613c","d":"heap","t":"Pattern 7 — Lazy deletion","f":"When is lazy deletion safe?","b":"When an entry, once invalid, **never becomes valid again** (window indices only move forward; Dijkstra distances only decrease)."},{"id":"ch-0621781b","d":"heap","t":"Pattern 7 — Lazy deletion","f":"Sliding Window Maximum with a heap — how are stale entries detected?","b":"Entries are `(-value, index)`; while the top's index `<= i - k` (outside the window) pop it."},{"id":"ch-93211bc3","d":"heap","t":"Pattern 7 — Lazy deletion","f":"Why does Dijkstra with `heapq` push duplicates and how are they handled?","b":"`heapq` has no decrease-key, so push the improved `(dist, node)`; when popping, skip any entry whose `dist != dist[node]` (stale)."},{"id":"ch-10620a34","d":"heap","t":"Pattern 7 — Lazy deletion","f":"Faster alternative for Sliding Window Maximum?","b":"A **monotonic deque** of indices (decreasing values): O(n) instead of the heap's O(n log n)."},{"id":"ch-bb70a7d2","d":"heap","t":"Pattern 8 — Hash map + heap","f":"Recognition signal for hash map + heap?","b":"You need **counts/frequencies first**, then the top K — or a heap combined with a structure that temporarily blocks an item."},{"id":"ch-8690d86a","d":"heap","t":"Pattern 8 — Hash map + heap","f":"Reorganize String (no two equal neighbours) — approach?","b":"Max-heap of `(−count, char)`; pop the most frequent, append it, and hold it back for one step (push it back after the next pop). Impossible if `max_count > (n+1)//2`."},{"id":"ch-aed74d62","d":"heap","t":"Pattern 8 — Hash map + heap","f":"Task Scheduler with cooldown — which structures?","b":"A **max-heap** of remaining counts (available now) + a **queue** of cooling-down tasks with the time they become usable. Idle when the heap is empty but the queue isn't."},{"id":"ch-796ea3a6","d":"heap","t":"Pattern 8 — Hash map + heap","f":"Closed form for Task Scheduler?","b":"`max(len(tasks), (max_count − 1) × (cooldown + 1) + number_of_tasks_with_max_count)`."},{"id":"ch-861599d7","d":"heap","t":"Decision guide","f":"You need repeated min/max of a changing set. → ?","b":"A **heap**."},{"id":"ch-f8575038","d":"heap","t":"Decision guide","f":"Only the K best elements. → ?","b":"A heap of **size K** (direction = who do I evict?)."},{"id":"ch-40aadd90","d":"heap","t":"Decision guide","f":"A running median. → ?","b":"**Two heaps** (max-heap lower half + min-heap upper half)."},{"id":"ch-02fd34b5","d":"heap","t":"Decision guide","f":"Several sorted sources. → ?","b":"**K-way merge** (one candidate per source)."},{"id":"ch-71c2803b","d":"heap","t":"Decision guide","f":"Intervals/events with a changing active set. → ?","b":"**Sort + heap** (heap of end times)."},{"id":"ch-12d3846b","d":"heap","t":"Decision guide","f":"Choices unlock over time and you take the best feasible. → ?","b":"**Heap + greedy** (sort by feasibility, max-heap of feasible choices)."},{"id":"ch-c540fce1","d":"heap","t":"Decision guide","f":"Elements become invalid but arbitrary deletion is awkward. → ?","b":"**Lazy deletion** (validate at the top)."},{"id":"ch-d535199a","d":"heap","t":"Decision guide","f":"The question to ask on any new heap problem?","b":"\"**What information do I repeatedly need from a changing set, and can a heap make exactly that operation cheap?**\""},{"id":"cs-e1d3246a","d":"stack","t":"Stack basics","f":"What does LIFO mean and what is the core question that makes a stack useful?","b":"Last In, First Out. A stack helps when **the most recently encountered unresolved thing must be handled before older unresolved things**."},{"id":"cs-e5107144","d":"stack","t":"Stack basics","f":"Stack vs queue — how do you decide?","b":"Ask: do I care about the **newest** pending item (stack) or the **oldest** (queue)?"},{"id":"cs-5fb05a87","d":"stack","t":"Stack basics","f":"A Python list as a stack — operations and costs?","b":"`append` (push, O(1) amortized), `pop()` (O(1)), `[-1]` (peek, O(1)). Never use `pop(0)`/`insert(0, x)` — they are **O(n)**."},{"id":"cs-61a201c5","d":"stack","t":"Stack basics","f":"What is a stack underflow, and how do you guard against it?","b":"Popping or peeking an empty stack (`IndexError` in Python). Guard with `while stack and ...` / `if stack`."},{"id":"cs-5637aa2a","d":"stack","t":"Stack basics","f":"Why does recursion relate to stacks?","b":"Each call pushes a frame on the **call stack** and each return pops it. Too-deep recursion → `RecursionError`; an explicit stack removes the limit."},{"id":"cs-6b04b78b","d":"stack","t":"Stack basics","f":"`collections.deque` vs `list` as a stack?","b":"Both O(1) at the end. `deque` gives O(1) *worst case* (no reallocation) and O(1) at both ends — needed for queues and monotonic deques."},{"id":"cs-835c0f24","d":"stack","t":"Stack basics","f":"The recognition checklist for a stack problem?","b":"\"What is each element **waiting for**? What event **resolves** it? Does the **newest** waiting element get resolved first?\" If yes → stack."},{"id":"cs-fbf098b3","d":"stack","t":"Matching and cancelling","f":"Valid Parentheses — algorithm?","b":"Push openers; on a closer the **top** must be its partner (pop it), else invalid; valid only if the stack is empty at the end."},{"id":"cs-5db06993","d":"stack","t":"Matching and cancelling","f":"When is a counter enough instead of a stack for brackets?","b":"With a **single** kind of bracket. A stack is needed when several kinds must nest correctly."},{"id":"cs-bda28dd8","d":"stack","t":"Matching and cancelling","f":"Longest Valid Parentheses — stack trick?","b":"A stack of **indices** seeded with `-1`. `(` push; `)` pop — if empty, push this index as the new base, else length = `i - stack[-1]`."},{"id":"cs-d71693cb","d":"stack","t":"Matching and cancelling","f":"Min Remove to Make Valid Parentheses — why store indices?","b":"To delete by position: push indices of `(`; an unmatched `)` is marked immediately; leftover indices on the stack are unmatched `(`."},{"id":"cs-250046cb","d":"stack","t":"Matching and cancelling","f":"Remove All Adjacent Duplicates — what does the stack represent?","b":"The surviving prefix. If the new character equals the top they cancel (pop); otherwise push. Chain reactions are automatic."},{"id":"cs-b32c495f","d":"stack","t":"Matching and cancelling","f":"Asteroid Collision — when does a collision happen and what are the outcomes?","b":"Only when the **top is positive** and the newcomer is **negative**. Top smaller → pop and continue; equal → both die; top larger → newcomer dies."},{"id":"cs-ccec05d0","d":"stack","t":"Matching and cancelling","f":"Score of Parentheses — what does the stack hold?","b":"The **score accumulated at each open depth**. `(` pushes 0; `)` pops `inner` and adds `max(2*inner, 1)` to the parent."},{"id":"cs-8751cd72","d":"stack","t":"Expressions and parsing","f":"RPN evaluation — which operand do you pop first?","b":"The **right** operand `b`, then the left `a`; push `a op b`. Matters for `-` and `/`. Division truncates toward zero: `int(a / b)`."},{"id":"cs-dfb21e56","d":"stack","t":"Expressions and parsing","f":"Basic Calculator II — what does the stack hold, and which operator do you apply when a number ends?","b":"The stack holds **signed terms**. When a number finishes apply the **previous** operator: `+`/`-` push the term, `*`/`/` fold into the top. Answer = `sum(stack)`."},{"id":"cs-39664527","d":"stack","t":"Expressions and parsing","f":"Basic Calculator (with parentheses) — what do you do on `(` and `)`?","b":"On `(` push `(result, sign)` and start fresh; on `)` finish the inner value, multiply by the saved sign, add the saved result."},{"id":"cs-cb851006","d":"stack","t":"Expressions and parsing","f":"Shunting-yard in one paragraph.","b":"Operands go to the output. An operator first pops operators with **higher or equal precedence** (left-associative) to the output, then is pushed. `(` is pushed; `)` pops until `(`. At the end pop everything."},{"id":"cs-06e91530","d":"stack","t":"Expressions and parsing","f":"Decode String `3[a2[c]]` — what does the stack store?","b":"`(text_before, repeat_count)`. `[` saves and resets; `]` resumes: `cur = text_before + cur * count`."},{"id":"cs-3e2c0171","d":"stack","t":"Expressions and parsing","f":"Simplify Path — which components push and pop?","b":"`..` pops (if any), `''` and `.` are ignored, everything else is pushed. Join the stack with `/`."},{"id":"cs-f4f93658","d":"stack","t":"Expressions and parsing","f":"Exclusive Time of Functions — what is the stack?","b":"The **call stack**: on `start` charge the running function, push the new one; on `end` charge and pop (end timestamps are inclusive, so resume at `ts + 1`)."},{"id":"cs-128b41f9","d":"stack","t":"Design problems","f":"Min Stack in O(1) — idea?","b":"Store `(value, min so far)` with every entry. `get_min` reads the top's min; popping restores the previous minimum automatically."},{"id":"cs-36e69288","d":"stack","t":"Design problems","f":"Queue using two stacks — algorithm and cost?","b":"Push to an **inbox**; pop from an **outbox**; only when the outbox is empty, move *everything* from the inbox (this reverses the order). Amortized **O(1)** per operation."},{"id":"cs-ee8bdb6d","d":"stack","t":"Design problems","f":"Stack using one queue?","b":"After each push, rotate the queue so the newest element is at the front (move the older n−1 behind it). push O(n), pop O(1)."},{"id":"cs-c9cf4d5c","d":"stack","t":"Design problems","f":"Design a stack with O(1) `increment(k, val)` — trick?","b":"**Lazy**: store `inc[k-1] += val`; when popping pass the pending increment down to the element beneath and add it to the returned value."},{"id":"cs-2bf1ebb9","d":"stack","t":"Design problems","f":"Validate Stack Sequences — algorithm?","b":"Push each element and pop **greedily** while the top equals the next expected pop. Valid iff the stack ends empty."},{"id":"cs-73ee7349","d":"stack","t":"Monotonic stack — core","f":"What does a monotonic stack maintain, in one sentence?","b":"The elements **still waiting** for their answer, kept in monotonic order so the loop can stop at the first element it cannot resolve."},{"id":"cs-c39ea975","d":"stack","t":"Monotonic stack — core","f":"Next Greater Element — pop condition, direction, invariant?","b":"Scan left → right; pop while `nums[i] > nums[top]`, setting `answer[j] = nums[i]`; stack values **decrease** bottom → top."},{"id":"cs-31acfc9e","d":"stack","t":"Monotonic stack — core","f":"Next Smaller Element — what changes?","b":"The comparison flips: pop while `nums[i] < nums[top]`; the stack values **increase** bottom → top."},{"id":"cs-70f74eee","d":"stack","t":"Monotonic stack — core","f":"The four variants in one table?","b":"Next Greater: pop `cur > top`, stack decreasing · Next Smaller: pop `cur < top`, increasing · Previous Greater: pop `top <= cur`, decreasing · Previous Smaller: pop `top >= cur`, increasing. *Greater/smaller picks the comparison; next/previous picks the direction.*"},{"id":"cs-3f58dc67","d":"stack","t":"Monotonic stack — core","f":"Form A vs Form B of the monotonic stack?","b":"**Form A**: the current element *resolves* older ones (`answer[j] = nums[i]` at pop time). **Form B**: the current element *asks* the stack (pop useless candidates, then `answer[i] = nums[top]`)."},{"id":"cs-14d07bc7","d":"stack","t":"Monotonic stack — core","f":"Why store indices, not values?","b":"To write the answer at the right position and to compute distances/widths. Compare with `nums[stack[-1]]`."},{"id":"cs-16e7d905","d":"stack","t":"Monotonic stack — core","f":"What do the elements left in the stack at the end mean?","b":"They never found a greater (or smaller) element → keep the default (`-1` / `0`)."},{"id":"cs-f76db6d3","d":"stack","t":"Monotonic stack — core","f":"Why is it O(n) even though there is a `while` inside a `for`?","b":"Every index is pushed **once** and popped **at most once** — total pops ≤ n."},{"id":"cs-a1b1746d","d":"stack","t":"Monotonic stack — core","f":"Why is checking only the top of the stack enough?","b":"The stack is ordered: once the current element cannot resolve the top, it cannot resolve anything below either."},{"id":"cs-221fb173","d":"stack","t":"Monotonic stack — core","f":"`>` vs `>=` in the pop condition — effect?","b":"With `>` equal values stay (stack **non-increasing**); with `>=` they are popped (strictly decreasing). Choose according to whether \"next greater\" is strict."},{"id":"cs-4f0d78dc","d":"stack","t":"Monotonic stack — core","f":"How do you handle a circular array in Next Greater?","b":"Iterate `i` from 0 to `2n − 1` using `nums[i % n]`, but **push indices only in the first pass**."},{"id":"cs-81a2e045","d":"stack","t":"Monotonic stack — core","f":"Daily Temperatures — how differs from Next Greater?","b":"The answer is the **index distance** `i - j`, so the stack must store indices."},{"id":"cs-cc257096","d":"stack","t":"Monotonic stack — core","f":"Online Stock Span — idea?","b":"Previous-greater with counts: stack of `(price, span)`; pop every entry with `price <= today` and add its span to today's span."},{"id":"cs-d8adb8bf","d":"stack","t":"Monotonic stack — core","f":"Remove K Digits — greedy rule?","b":"A bigger digit **before** a smaller one is wasteful: pop while `top > digit` and removals remain; drop leftovers from the end; strip leading zeros."},{"id":"cs-e92c2d56","d":"stack","t":"Monotonic stack — core","f":"Remove Duplicate Letters — extra rule vs Remove K Digits?","b":"Skip letters already in the result, and only pop the top if it **appears again later** (`last[top] > i`)."},{"id":"cs-ff939feb","d":"stack","t":"Monotonic stack — core","f":"132 Pattern — direction and idea?","b":"Scan from the **right** with a decreasing stack; values popped by a larger `x` become the best \"2\" (`third`); any later `x < third` completes the pattern."},{"id":"cs-633df870","d":"stack","t":"Areas and contributions","f":"Largest Rectangle in Histogram — what does popping a bar mean?","b":"Its **maximum width has been determined**: the arriving (shorter) bar is the right boundary, the element now exposed underneath is the left boundary → `width = i - left - 1`."},{"id":"cs-19628543","d":"stack","t":"Areas and contributions","f":"What invariant does the histogram stack keep?","b":"**Increasing** heights bottom → top: each bar on the stack can still extend to the right because everything since is ≥ it."},{"id":"cs-cc4b5caf","d":"stack","t":"Areas and contributions","f":"Why append a `0` sentinel to the heights?","b":"A height-0 bar forces every remaining bar to be popped and measured, so no cleanup loop is needed."},{"id":"cs-bd29a291","d":"stack","t":"Areas and contributions","f":"Maximal Rectangle — reduction?","b":"Build a histogram per row (height = consecutive 1s ending at that row, reset at `'0'`) and run Largest Rectangle on each row."},{"id":"cs-5932023f","d":"stack","t":"Areas and contributions","f":"Trapping Rain Water with a stack — when is a pool closed and how is it measured?","b":"When a taller bar arrives: pop the **floor**, the new top is the left wall; add `width × (min(h, left wall) − floor)`. Decreasing stack."},{"id":"cs-450ec593","d":"stack","t":"Areas and contributions","f":"Sum of Subarray Minimums — contribution technique?","b":"For each element count subarrays where it is the minimum: `left[i] × right[i]` with `left` = distance to previous smaller, `right` = distance to next smaller; sum `arr[i] × left × right`."},{"id":"cs-3dd23359","d":"stack","t":"Areas and contributions","f":"How do you avoid double-counting equal values in that technique?","b":"Use **strict** comparison on one side and **non-strict** on the other, so each subarray is attributed to exactly one (leftmost) minimum."},{"id":"cs-a609796b","d":"stack","t":"Areas and contributions","f":"Sum of Subarray Ranges — trick?","b":"`Σ(max − min) = Σ max − Σ min` — run the contribution technique twice."},{"id":"cs-170c0041","d":"stack","t":"Stack of groups, DFS, deque","f":"Car Fleet — what does the stack hold and what is the rule?","b":"**Arrival times of fleets**. Sort cars by position from the target; if a car's time is **greater** than the stack top it forms a new fleet (push), else it merges (do nothing)."},{"id":"cs-cb348629","d":"stack","t":"Stack of groups, DFS, deque","f":"Why doesn't a merged car change the fleet's time in Car Fleet?","b":"The fleet travels at the slower car's speed, so it arrives at the time already on the stack."},{"id":"cs-ce4d4b95","d":"stack","t":"Stack of groups, DFS, deque","f":"Iterative in-order traversal — algorithm?","b":"Go as far left as possible pushing nodes; pop → visit → move to the right child; repeat while the node or the stack is non-empty."},{"id":"cs-4fbf5a59","d":"stack","t":"Stack of groups, DFS, deque","f":"Iterative pre-order and post-order tricks?","b":"Pre-order: pop, visit, push **right then left**. Post-order: produce *node, right, left* (mirrored pre-order) and **reverse** it."},{"id":"cs-8dd40c2d","d":"stack","t":"Stack of groups, DFS, deque","f":"Iterative DFS on a graph — skeleton?","b":"`stack = [start]; while stack: node = stack.pop(); if node in visited: continue; visited.add(node); visit; push neighbours` (reversed for listed order)."},{"id":"cs-ce80ab85","d":"stack","t":"Stack of groups, DFS, deque","f":"Why convert recursion to an explicit stack?","b":"Avoid `RecursionError` on deep inputs and control the order/pausing; push what the recursive call would have remembered."},{"id":"cs-2d36fc17","d":"stack","t":"Stack of groups, DFS, deque","f":"Monotonic deque for Sliding Window Maximum — rules?","b":"Deque of indices with **decreasing** values. Pop from the back while the new value is bigger; pop the front if its index `<= i - k`; the front is the window maximum. O(n)."},{"id":"cs-ebafc2c5","d":"stack","t":"Decision guide","f":"Brackets, tags, nested calls → ?","b":"A **matching stack** (push openers, pop on closers)."},{"id":"cs-a39cd618","d":"stack","t":"Decision guide","f":"\"Nearest greater/smaller to the left/right\", \"days until warmer\" → ?","b":"**Monotonic stack**."},{"id":"cs-548c97c8","d":"stack","t":"Decision guide","f":"Widest range where an element is the min/max (areas, sums of minimums) → ?","b":"Monotonic stack giving **both boundaries**."},{"id":"cs-895ea897","d":"stack","t":"Decision guide","f":"min/max/undo of a stack in O(1) → ?","b":"**Design**: store extra information with every element."},{"id":"cs-fee44335","d":"stack","t":"Decision guide","f":"Groups that merge when one catches up → ?","b":"A **stack of groups** (Car Fleet)."},{"id":"cs-d8fec797","d":"stack","t":"Decision guide","f":"Sliding-window maximum → ?","b":"A **monotonic deque**."},{"id":"cs-32db0252","d":"stack","t":"Decision guide","f":"The single sentence to remember for stacks?","b":"\"**What is waiting, and what event resolves it?** Do newer waiting elements get resolved first? What invariant does the stack keep, and what does POP mean?\""}]}